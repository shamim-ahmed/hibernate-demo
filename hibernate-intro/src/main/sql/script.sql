CREATE TABLE MessageOfDay(
  id INTEGER NOT NULL PRIMARY KEY AUTO_INCREMENT,
  message VARCHAR(250) NOT NULL
);

INSERT INTO MessageOfDay(message) VALUES ('In God we trust, others must pay cash');
INSERT INTO MessageOfDay(message) VALUES ('There is no place like www.home.com');