CREATE TABLE MessageOfDay(
  id INTEGER NOT NULL PRIMARY KEY,
  message VARCHAR(250) NOT NULL
);

INSERT INTO MessageOfDay(id, message) VALUES (1, 'In God we trust, others must pay cash');
INSERT INTO MessageOfDay(id, message) VALUES (2, 'There is no place like www.home.com');